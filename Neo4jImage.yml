version: '2'

services:
  neo4j:
    image: neo4j
    container_name: "neo4j-user"
    hostname: localhost
    environment:
      NEO4J_AUTH: neo4j/admin
    ports:
      - 7474:7474
      - 7687:7687
    restart: always
